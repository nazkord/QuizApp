<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nazar.myfirstapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/title1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tabs.TabbedActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.FirstActivity" />
        <activity android:name=".activities.ThirdActivity" />
        <activity android:name=".activities.FourthActivity" />
        <activity android:name=".activities.FifthActivity" />
        <activity
            android:name=".activities.SixthActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".activities.editableActivity" />
    </application>

</manifest>